# 수정사항 기록

---

## [2026-04-02] Phase 2 주제 추천 결과 DB 저장 로직 추가

### 변경 파일
- `backend/app/routers/ai.py`

### 변경 내용

**routers/ai.py**
- `recommend_topics` 함수 내에서 AI가 생성한 3가지 추천 주제 리스트를 `AIContext` 테이블에 저장하는 로직을 추가하였습니다.
- `context_type`을 `topic_recommendation`으로 설정하고, `context_json` 필드에 추천된 데이터를 JSON 형태로 보관합니다.
- 새로운 추천이 생성될 때 기존의 활성 상태인 추천 내역을 `is_active=False`로 업데이트하여 최신 상태를 유지합니다.

---


### 변경 파일
- `backend/app/routers/ai.py`

### 변경 내용

**routers/ai.py**
- 로컬에서 추가된 `SUBJECT_QUESTIONS` (과목별 질문 목록)와 원격에서 업데이트된 아이스브레이킹 헬퍼 함수 (`build_summary_text_from_context_json`, `build_system_ice_breaking_prompt` 등) 간의 병합 충돌을 해결하였습니다.
- `/ice-breaking` API의 데코레이터와 함수 시그니처를 상세 설명이 포함된 원격 버전으로 업데이트하였습니다.
- 두 수정의 의도를 모두 반영하여, 과목별 주제 추천 기능과 강화된 아이스브레이킹 로직이 공존하도록 처리하였습니다.

### 새 테이블 추가 여부
- **없음**

---


---

## [2026-04-02] POST /api/ai/topics 스키마 변경 및 질문 하드코딩

### 변경 파일
- `backend/app/schemas.py`
- `backend/app/routers/ai.py`

### 변경 내용

**schemas.py**
- `TopicRecommendRequest` 구조 변경
  - 기존: `{ room_id, subject, answers: List[IceBreakingAnswer] }`
  - 변경: `{ room_id, subject, member_answers: List[MemberAnswer] }`
  - `MemberAnswer`: `{ user_id, answers: List[str] }` — 질문 순서에 맞춰 index 대응
- 신규 스키마 추가
  - `QuestionItem`: 질문 1개 단위 (index, text, type, multiple, options)
  - `SubjectQuestionsResponse`: 과목 + 질문 목록 + 팁

**routers/ai.py**
- `SUBJECT_QUESTIONS` dict 하드코딩: 3개 과목 × 5개 질문 + 과목별 팁 포함
  - 디자인적 사고
  - 세계와 시민
  - 데이터분석캡스톤디자인
- `GET /api/ai/topics/questions?subject=...` 엔드포인트 추가
  - 프론트엔드가 질문 목록을 fetch하기 위해 먼저 호출하는 용도
- `POST /api/ai/topics` 로직 전면 수정
  - 과목 유효성 검증 추가
  - 팀원별 답변 집계 후 LLM 호출
  - 추천 주제 **5개 → 3개**로 변경
  - 과목별 팁을 system prompt에 포함

### 새 테이블 추가 여부
- **없음** (단발성 방식 유지)

---

## [2026-04-02] POST /api/ai/topics 최초 구현

### 변경 파일
- `backend/app/routers/ai.py`

### 변경 내용

`recommend_topics` 함수에 실제 로직을 구현하였습니다. (기존에는 빈 배열만 반환하는 stub 상태였음)

**처리 흐름:**
1. `room_id`로 룸 존재 여부 확인 (없으면 404 반환)
2. DB에서 룸 멤버 프로필 조회 (`users` → `room_members` → `user_profiles` JOIN)
   - 활용 필드: MBTI, 전공(major), 역할(role), 취미(hobby)
3. 요청으로 넘어온 아이스브레이킹 `answers` 정리
4. System Prompt + User Prompt 구성 → LLM(claude-sonnet-4-6) 호출
   - LLM에게 순수 JSON 배열 형태로만 응답하도록 지시
5. LLM 응답을 `json.loads`로 파싱 → `RecommendedTopic` 리스트로 변환
6. `TopicRecommendResponse` 반환

**에러 처리:**
- LLM API 호출 실패 → 502 반환
- JSON 파싱 실패 → 500 + 원본 응답 일부 반환

### 새 테이블 추가 여부
- **없음** (단발성 방식으로 구현, 결과를 DB에 저장하지 않음)

---

## [2026-04-02] POST /api/ai/topics 구현

### 변경 파일
- `backend/app/routers/ai.py`

### 변경 내용

`recommend_topics` 함수에 실제 로직을 구현하였습니다. (기존에는 빈 배열만 반환하는 stub 상태였음)

**처리 흐름:**
1. `room_id`로 룸 존재 여부 확인 (없으면 404 반환)
2. DB에서 룸 멤버 프로필 조회 (`users` → `room_members` → `user_profiles` JOIN)
   - 활용 필드: MBTI, 전공(major), 역할(role), 취미(hobby)
3. 요청으로 넘어온 아이스브레이킹 `answers` 정리
4. System Prompt + User Prompt 구성 → LLM(claude-sonnet-4-6) 호출
   - LLM에게 순수 JSON 배열 형태로만 응답하도록 지시
5. LLM 응답을 `json.loads`로 파싱 → `RecommendedTopic` 리스트로 변환
6. `TopicRecommendResponse` 반환

**에러 처리:**
- LLM API 호출 실패 → 502 반환
- JSON 파싱 실패 → 500 + 원본 응답 일부 반환

### 새 테이블 추가 여부
- **없음** (단발성 방식으로 구현, 결과를 DB에 저장하지 않음)
